# SirsiDynexFunctionalTests
During migration to the new search, some end-to-end tests to track what works and what we broke.

run on the command line with `pytest`  or a specific file with `pytest {filename}`
